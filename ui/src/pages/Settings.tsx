export function Settings() {
  return (
    <main className="content">
      <header className="page-header">
        <h1>Settings</h1>
      </header>
      <p className="muted">
        Settings live here. Daemon address, tracked repositories, and indexing
        options will move into this panel as configuration migrates to the
        database.
      </p>
    </main>
  );
}
