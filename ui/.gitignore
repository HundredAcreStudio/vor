node_modules/

# NB: dist/ IS committed. The vor binary embeds it via go:embed, so a plain
# `go build` (no Node) must always find a real, self-consistent bundle.
# Regenerate with `make ui` and commit the result when the UI changes.
